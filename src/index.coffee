Context = require "./context"

class Capirona 
  
  ###
  ###

  run: (source, context, callback) ->



exports.run = (source, context, callback) -> new Capirona().run(source, context, callback)

exports.Context = Context